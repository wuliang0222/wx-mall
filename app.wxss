/* 导入项目小图标样式 */
@import "./styles/iconfont.wxss";

/* 初始化全局样式 */
page,
view,
text,
swiper,
swiper-item,
image,
navigator {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

page {
  /* 主题颜色 */
  --themeColor: #FF5700;
  /* 字体大小 rpx自适应大小 */
  font-size: 28rpx;
}

image {
  width: 100%;
}